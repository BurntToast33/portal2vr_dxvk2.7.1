V0.2.3
- Test co-op
- Add splitscreen support
- Add gesture support


V0.2.2
Todo:
- Update to dxvk 3.0
- Finish Overlay based aiming
- Fix MSAA (I don't know how, everything is transparent so run without msaa for now)
- Add cube map transisions between levels to stay in the aptiure envrionment while loading
- ask about adding Spencer0187's roomscale code (see about adding the ability to walk through portals)
- Add 3rd portal support for P2:Reloaded
- Add smooth rotation through portals


Todo:
- Wait on feedback from playtesters
- Fix blackscreen issue with HoriZon


V0.2.1
Done:
- Fixed some maps setting a invalid render state when compositing hud onto spectator cam causing a black screen.
- Overhalled how backgrounds file is parsed to make way for mapping cube maps to loading zones in the future.
- Disabled msaa due to being broken right now.
- Added levels to debug command `-vrdebug`, `-vrdebug 1` opens console and logs more detail, `-vrdebug 2`  adds compositer error logging.


V0.2.1-preview
Done:
- Updated to DXVK 2.7.1. (+FPS)
- Fixed function hook offsets for the current Portal 2 build (as of July 7, 2026).
- Fixed a crash in `sp_a2_bts4`.
- Fixed the ending: credits now render to the HUD in-game.
- Fixed a crash caused by laser pointer particles on creation.
- Fixed a possible soft lock when exiting a level.
- Fixed a potential memory leak caused by old render targets not being released.
- Fixed an issue introduced by the current game build where viewmodel particle attachment points were transformed incorrectly.
- The 2D menu now spawns based on your VR environment's forward direction.
- Added the `m_RenderWindow` setting to copy the left eye to the back buffer instead of rendering a third view. (+FPS)
- Force-disabled motion blur in VR mode. (+FPS)
- Moved `mat_*` commands from launch arguments to `VR_autoexec.cfg` (located in `portal2_dlc3/cfg/VR_autoexec.cfg`).
- Updated console colors for warnings and errors.
- Replaced the portal gun model with one by SpiritedSpy.
- Compress ViewmodelPosCustomOffset and ViewmodelAngCustomOffset into 1 line each instead of 3 each (config file)
- Got P2VR working on linux using proton
- Moved Controller settings to KeyboardMouse.res layout style (Requires updated portal2_dlc3 file)

- Added 3D backgrounds. (To add custom backgrounds, see `VR/backgrounds.txt` for the mapping format.)
- Added a 3D in-level menu.
- Added the `-vr` launch option to enable or disable VR mode and VR assets.
- Added the `-vrdebug` launch option to enable the debug console.
- Added automatic recentering when loading into a map.
- Added error logging from the Left 4 Dead 2 branch. Sessions now generate `vrmod_log.txt`.
- Added the `ExperimentalOptimizations` setting to share visibility data between both eyes.
- Added HUD compositing for spectators and the VR player.
- Added a VR HUD.
- Added VR settings to the in-game Options menu.
- Added a button to open the controller remapping UI from the VR settings menu.

- Overhauled the overlay repositioning system.
- Removed the old HUD system to make way for a new capture method.
- Reworked texture capture.
- Overhauled the VGUI capture system to support HUD rendering.
- Added an automatic system to compare defined offsets against those found in the binary.
- Added utilities for finding functions in binaries and other development tools.
- Cleaned up and reorganized the hook files.
- Cleaned up and organized the offsets file.
- Added systems for overriding layouts, overriding panel settings, and listening to the panel command stream.